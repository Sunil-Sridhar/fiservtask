import { DocumentSearchPipe } from './category-pipe';

describe('DocumentSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
